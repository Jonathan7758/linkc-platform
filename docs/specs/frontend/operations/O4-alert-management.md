# O4 告警管理界面规格书

## 文档信息

| 属性 | 值 |
|-----|-----|
| 模块ID | O4 |
| 模块名称 | 告警管理界面 (Alert Management) |
| 版本 | 1.0 |
| 日期 | 2026-01-20 |
| 状态 | 规划中 |
| 所属终端 | 运营控制台 (Operations Console) |
| 前置依赖 | G6-data-api, A1-agent-runtime |

---

## 一、模块概述

### 1.1 职责描述

告警管理界面是运营控制台的重要组件，为运营团队提供系统告警的集中监控、处理和分析能力，确保异常情况得到及时响应和处理。

### 1.2 核心功能

| 功能 | 描述 | 优先级 |
|-----|------|-------|
| 告警列表 | 查看和筛选所有告警 | P0 |
| 告警详情 | 查看告警详细信息和上下文 | P0 |
| 告警处理 | 确认、处理、关闭告警 | P0 |
| 告警统计 | 告警趋势和分布分析 | P1 |
| 告警规则 | 配置告警规则和阈值 | P1 |
| 通知设置 | 配置告警通知方式和接收人 | P2 |

### 1.3 告警分级

| 级别 | 名称 | 颜色 | 响应时间 | 示例 |
|-----|------|------|---------|------|
| P0 | 紧急 | 红色 | 5分钟 | 机器人碰撞、系统宕机 |
| P1 | 严重 | 橙色 | 15分钟 | 机器人卡住、任务失败 |
| P2 | 警告 | 黄色 | 1小时 | 电量低、耗材不足 |
| P3 | 提示 | 蓝色 | 4小时 | 效率下降、非关键异常 |

---

## 二、页面结构

### 2.1 主页面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [告警管理]                                          [告警规则] [通知设置]  │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ 告警概览                                                              │  │
│  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐          │  │
│  │ │ 待处理  │ │ 紧急    │ │ 严重    │ │ 警告    │ │ 今日新增│          │  │
│  │ │  12     │ │   2     │ │   5     │ │   5     │ │   18    │          │  │
│  │ │ ●活跃   │ │ ●P0     │ │ ●P1     │ │ ●P2     │ │ ↑5      │          │  │
│  │ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘          │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ [待处理] [处理中] [已解决] [全部]                                     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ [级别▼] [类型▼] [来源▼] [时间▼] [搜索...]           [批量处理] [导出]│  │
│  └───────────────────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ 告警列表                                                              │  │
│  │ ┌─────────────────────────────────────────────────────────────────────┐│  │
│  │ │ □ | 级别 | 告警内容 | 来源 | 时间 | 状态 | 处理人 | 操作           ││  │
│  │ ├─────────────────────────────────────────────────────────────────────┤│  │
│  │ │ □ | 🔴P0 | 机器人GX-003碰撞停机 | GX-003 | 08:15 | 待处理 | -- | ⋮ ││  │
│  │ │ □ | 🟠P1 | 任务T-012执行超时 | Agent | 08:10 | 处理中 | 张三 | ⋮   ││  │
│  │ │ □ | 🟠P1 | 机器人EC-002卡住 | EC-002 | 08:05 | 待处理 | -- | ⋮    ││  │
│  │ │ □ | 🟡P2 | 机器人GX-001电量低(15%) | GX-001 | 08:00 | 待处理 | -- |││  │
│  │ │ □ | 🟡P2 | 滤网寿命不足20% | GX-005 | 07:55 | 待处理 | -- | ⋮      ││  │
│  │ │ □ | 🔵P3 | A栋清洁效率下降10% | Agent | 07:50 | 已确认 | 李四 | ⋮  ││  │
│  │ └─────────────────────────────────────────────────────────────────────┘│  │
│  │ 共 48 条告警  < 1 2 3 4 5 >  每页显示 [20▼]                            │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 告警详情面板

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  告警详情                                                            [×]    │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 🔴 P0 紧急 | 机器人GX-003碰撞停机                                   │   │
│  │ 告警ID: ALT-2026012008150001                                        │   │
│  │ 状态: [待处理]                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  基本信息                                                                   │
│  ├─ 告警类型: 机器人异常 > 碰撞检测                                        │
│  ├─ 告警来源: 机器人 GX-003 (高仙X100)                                     │
│  ├─ 发生时间: 2026-01-20 08:15:32                                          │
│  ├─ 发生位置: A栋 2层 走廊区域                                             │
│  └─ 关联任务: T-008 A栋2F走廊日常清洁                                      │
│                                                                             │
│  告警描述                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 机器人GX-003在执行清洁任务时检测到碰撞，已自动停机。                 │   │
│  │ 碰撞传感器触发，位置：坐标(125.3, 87.6)                             │   │
│  │ 可能原因：路径上有障碍物或地图数据过期                               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  相关数据                                                                   │
│  ├─ 碰撞前速度: 0.5 m/s                                                    │
│  ├─ 碰撞力度: 中等                                                         │
│  ├─ 机器人电量: 62%                                                        │
│  └─ 任务进度: 45%                                                          │
│                                                                             │
│  建议操作                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 1. 现场检查机器人和碰撞位置                                          │   │
│  │ 2. 清除障碍物或更新地图数据                                          │   │
│  │ 3. 重启机器人并恢复任务                                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  处理操作                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ [确认告警] [开始处理] [指派他人] [关联工单] [标记误报]               │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  处理记录                                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 暂无处理记录                                                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、组件定义

### 3.1 告警数据模型

```typescript
// 告警接口定义
interface Alert {
  id: string;
  level: AlertLevel;
  type: AlertType;
  title: string;
  description: string;
  source: AlertSource;
  status: AlertStatus;
  createdAt: string;
  updatedAt: string;
  acknowledgedAt?: string;
  acknowledgedBy?: string;
  resolvedAt?: string;
  resolvedBy?: string;
  resolution?: string;
  metadata: Record<string, any>;
  relatedEntities: {
    robotId?: string;
    taskId?: string;
    zoneId?: string;
    agentId?: string;
  };
}

// 告警级别
type AlertLevel = 'P0' | 'P1' | 'P2' | 'P3';

// 告警类型
type AlertType = 
  | 'robot.collision'
  | 'robot.stuck'
  | 'robot.offline'
  | 'robot.low_battery'
  | 'robot.error'
  | 'robot.consumable_low'
  | 'task.timeout'
  | 'task.failed'
  | 'task.abnormal'
  | 'agent.escalation'
  | 'agent.decision_failed'
  | 'system.error'
  | 'system.performance';

// 告警来源
interface AlertSource {
  type: 'robot' | 'agent' | 'task' | 'system';
  id: string;
  name: string;
}

// 告警状态
type AlertStatus = 
  | 'pending'       // 待处理
  | 'acknowledged'  // 已确认
  | 'in_progress'   // 处理中
  | 'resolved'      // 已解决
  | 'closed'        // 已关闭
  | 'false_alarm';  // 误报
```

### 3.2 告警概览组件 (AlertOverview)

```typescript
interface AlertOverviewProps {
  stats: AlertStats;
  onStatClick: (filter: AlertFilter) => void;
}

interface AlertStats {
  pending: number;
  p0: number;
  p1: number;
  p2: number;
  p3: number;
  todayNew: number;
  todayResolved: number;
  avgResponseTime: number;  // 分钟
}

const levelConfig = {
  P0: { label: '紧急', color: '#f5222d', icon: 'alert' },
  P1: { label: '严重', color: '#fa8c16', icon: 'warning' },
  P2: { label: '警告', color: '#fadb14', icon: 'info-circle' },
  P3: { label: '提示', color: '#1890ff', icon: 'bulb' }
};
```

### 3.3 告警列表组件 (AlertList)

```typescript
interface AlertListProps {
  alerts: Alert[];
  loading: boolean;
  pagination: PaginationConfig;
  onAlertClick: (alertId: string) => void;
  onAlertAction: (alertId: string, action: AlertAction) => void;
  onSelectionChange: (selectedIds: string[]) => void;
  onPageChange: (page: number, pageSize: number) => void;
}

type AlertAction = 
  | 'acknowledge'   // 确认
  | 'start_handle'  // 开始处理
  | 'resolve'       // 解决
  | 'close'         // 关闭
  | 'mark_false'    // 标记误报
  | 'assign';       // 指派

// 列表列配置
const columns = [
  { key: 'level', title: '级别', width: 80, render: LevelTag },
  { key: 'title', title: '告警内容', flex: 1 },
  { key: 'source', title: '来源', width: 120 },
  { key: 'createdAt', title: '时间', width: 150, render: TimeAgo },
  { key: 'status', title: '状态', width: 100, render: StatusTag },
  { key: 'acknowledgedBy', title: '处理人', width: 100 },
  { key: 'actions', title: '操作', width: 120, render: ActionMenu }
];
```

### 3.4 告警筛选组件 (AlertFilter)

```typescript
interface AlertFilterProps {
  filters: AlertFilters;
  onChange: (filters: AlertFilters) => void;
  onReset: () => void;
}

interface AlertFilters {
  level?: AlertLevel[];
  type?: AlertType[];
  status?: AlertStatus[];
  sourceType?: string[];
  dateRange?: [string, string];
  keyword?: string;
}

// 快捷筛选按钮
const quickFilters = [
  { key: 'pending', label: '待处理', filter: { status: ['pending'] } },
  { key: 'in_progress', label: '处理中', filter: { status: ['in_progress', 'acknowledged'] } },
  { key: 'resolved', label: '已解决', filter: { status: ['resolved', 'closed'] } },
  { key: 'all', label: '全部', filter: {} }
];
```

### 3.5 告警详情组件 (AlertDetail)

```typescript
interface AlertDetailProps {
  alertId: string;
  onClose: () => void;
  onAction: (action: AlertAction, params?: any) => void;
}

interface AlertDetailData extends Alert {
  // 建议操作
  suggestedActions: {
    action: string;
    description: string;
  }[];
  // 处理记录
  handleLogs: {
    time: string;
    operator: string;
    action: string;
    comment?: string;
  }[];
  // 相关告警
  relatedAlerts: Alert[];
  // 历史类似告警
  similarAlerts: {
    count: number;
    lastOccurrence: string;
    commonResolution?: string;
  };
}
```

### 3.6 告警处理弹窗 (AlertHandleModal)

```typescript
interface AlertHandleModalProps {
  visible: boolean;
  alert: Alert;
  action: AlertAction;
  onSubmit: (params: HandleParams) => Promise<void>;
  onCancel: () => void;
}

interface HandleParams {
  action: AlertAction;
  comment?: string;
  resolution?: string;
  assignTo?: string;
  workOrderId?: string;
}
```

**处理弹窗布局**：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  处理告警                                                            [×]    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  告警: 机器人GX-003碰撞停机                                                 │
│  操作: [解决告警]                                                           │
│                                                                             │
│  处理结果*                                                                  │
│  ○ 已修复 - 问题已解决                                                      │
│  ○ 临时处理 - 已临时处理，需后续跟进                                        │
│  ● 根本解决 - 已找到并解决根本原因                                          │
│  ○ 无法复现 - 问题无法复现                                                  │
│                                                                             │
│  解决方案描述*                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 已清除路径障碍物（一个纸箱），更新了地图数据，机器人已恢复正常运行。 │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  [ ] 创建预防工单 - 防止类似问题再次发生                                    │
│  [ ] 通知相关人员                                                           │
│                                                                             │
│                                              [取消]  [确认处理]             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.7 告警规则配置 (AlertRuleConfig)

```typescript
interface AlertRule {
  id: string;
  name: string;
  description: string;
  enabled: boolean;
  condition: AlertCondition;
  level: AlertLevel;
  notification: NotificationConfig;
  createdAt: string;
  updatedAt: string;
}

interface AlertCondition {
  type: string;
  metric: string;
  operator: 'gt' | 'lt' | 'eq' | 'gte' | 'lte';
  threshold: number;
  duration?: number;  // 持续时间(秒)
  scope?: {
    buildingIds?: string[];
    robotIds?: string[];
    taskTypes?: string[];
  };
}

interface NotificationConfig {
  channels: ('email' | 'sms' | 'push' | 'webhook')[];
  recipients: string[];
  escalation?: {
    delay: number;  // 分钟
    level: AlertLevel;
    recipients: string[];
  };
}
```

**规则配置页面布局**：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  告警规则配置                                                  [+ 新建规则] │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 规则名称    | 触发条件           | 级别 | 通知 | 状态  | 操作          │ │
│ ├─────────────────────────────────────────────────────────────────────────┤ │
│ │ 机器人碰撞  | 碰撞传感器触发      | P0   | 全部 | ●启用 | 编辑 删除    │ │
│ │ 机器人卡住  | 位置不变>5分钟      | P1   | 短信 | ●启用 | 编辑 删除    │ │
│ │ 低电量警告  | 电量<20%           | P2   | 推送 | ●启用 | 编辑 删除    │ │
│ │ 任务超时    | 执行时间>预计*1.5   | P1   | 邮件 | ●启用 | 编辑 删除    │ │
│ │ 耗材不足    | 耗材寿命<20%       | P2   | 邮件 | ●启用 | 编辑 删除    │ │
│ │ 效率下降    | 覆盖率<目标*0.8    | P3   | 无   | ○禁用 | 编辑 删除    │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.8 告警统计组件 (AlertStatistics)

```typescript
interface AlertStatisticsProps {
  dateRange: [string, string];
  onDateRangeChange: (range: [string, string]) => void;
}

interface AlertStatisticsData {
  // 按时间趋势
  trend: {
    date: string;
    total: number;
    p0: number;
    p1: number;
    p2: number;
    p3: number;
  }[];
  // 按类型分布
  byType: {
    type: AlertType;
    count: number;
    percentage: number;
  }[];
  // 按来源分布
  bySource: {
    source: string;
    count: number;
  }[];
  // 响应时间分析
  responseTime: {
    avgAcknowledge: number;  // 平均确认时间(分钟)
    avgResolve: number;      // 平均解决时间(分钟)
    withinSLA: number;       // SLA达成率
  };
  // Top告警
  topAlerts: {
    type: AlertType;
    count: number;
    avgResolveTime: number;
  }[];
}
```

---

## 四、数据流设计

### 4.1 状态管理

```typescript
// Alert Store (Zustand)
interface AlertStore {
  // 状态
  alerts: Alert[];
  selectedAlert: Alert | null;
  alertDetail: AlertDetailData | null;
  filters: AlertFilters;
  pagination: PaginationConfig;
  loading: boolean;
  error: string | null;
  
  // 统计
  stats: AlertStats;
  
  // 实时告警
  newAlertCount: number;  // 新告警数（未刷新）
  
  // Actions
  fetchAlerts: (filters?: AlertFilters) => Promise<void>;
  fetchAlertDetail: (alertId: string) => Promise<void>;
  handleAlert: (alertId: string, action: AlertAction, params?: any) => Promise<void>;
  batchHandle: (alertIds: string[], action: AlertAction) => Promise<void>;
  
  // 筛选
  setFilters: (filters: AlertFilters) => void;
  resetFilters: () => void;
  
  // 实时更新
  addNewAlert: (alert: Alert) => void;
  updateAlert: (alertId: string, updates: Partial<Alert>) => void;
  
  // 分页
  setPage: (page: number) => void;
}

// Alert Rules Store
interface AlertRuleStore {
  rules: AlertRule[];
  loading: boolean;
  
  fetchRules: () => Promise<void>;
  createRule: (rule: Partial<AlertRule>) => Promise<void>;
  updateRule: (ruleId: string, updates: Partial<AlertRule>) => Promise<void>;
  deleteRule: (ruleId: string) => Promise<void>;
  toggleRule: (ruleId: string, enabled: boolean) => Promise<void>;
}
```

### 4.2 API调用

```typescript
const alertApi = {
  // 获取告警列表
  getAlerts: (params: AlertQueryParams): Promise<PaginatedResponse<Alert>> =>
    api.get('/api/v1/alerts', { params }),
  
  // 获取告警详情
  getAlertDetail: (alertId: string): Promise<AlertDetailData> =>
    api.get(`/api/v1/alerts/${alertId}`),
  
  // 获取告警统计
  getAlertStats: (): Promise<AlertStats> =>
    api.get('/api/v1/alerts/stats'),
  
  // 处理告警
  handleAlert: (alertId: string, params: HandleParams): Promise<void> =>
    api.post(`/api/v1/alerts/${alertId}/handle`, params),
  
  // 批量处理
  batchHandle: (alertIds: string[], action: AlertAction): Promise<void> =>
    api.post('/api/v1/alerts/batch', { alert_ids: alertIds, action }),
  
  // 获取统计数据
  getStatistics: (params: { start: string; end: string }): Promise<AlertStatisticsData> =>
    api.get('/api/v1/alerts/statistics', { params }),
  
  // 告警规则
  getRules: (): Promise<AlertRule[]> =>
    api.get('/api/v1/alert-rules'),
  
  createRule: (rule: Partial<AlertRule>): Promise<AlertRule> =>
    api.post('/api/v1/alert-rules', rule),
  
  updateRule: (ruleId: string, updates: Partial<AlertRule>): Promise<AlertRule> =>
    api.patch(`/api/v1/alert-rules/${ruleId}`, updates),
  
  deleteRule: (ruleId: string): Promise<void> =>
    api.delete(`/api/v1/alert-rules/${ruleId}`),
};
```

### 4.3 实时告警推送

```typescript
// WebSocket订阅告警
const useAlertUpdates = () => {
  const addNewAlert = useAlertStore(state => state.addNewAlert);
  const updateAlert = useAlertStore(state => state.updateAlert);
  
  useEffect(() => {
    const ws = new WebSocket(`${WS_BASE_URL}/alerts`);
    
    ws.onmessage = (event) => {
      const message: AlertMessage = JSON.parse(event.data);
      
      switch (message.type) {
        case 'alert.new':
          // 新告警
          addNewAlert(message.alert);
          // 播放提示音（根据级别）
          playAlertSound(message.alert.level);
          // 显示桌面通知
          showDesktopNotification(message.alert);
          break;
          
        case 'alert.updated':
          updateAlert(message.alertId, message.updates);
          break;
          
        case 'alert.resolved':
          updateAlert(message.alertId, { 
            status: 'resolved',
            resolvedAt: message.resolvedAt,
            resolvedBy: message.resolvedBy 
          });
          break;
      }
    };
    
    return () => ws.close();
  }, []);
};

// 告警提示音
const playAlertSound = (level: AlertLevel) => {
  const sounds = {
    P0: '/sounds/alert-critical.mp3',
    P1: '/sounds/alert-high.mp3',
    P2: '/sounds/alert-warning.mp3',
    P3: '/sounds/alert-info.mp3'
  };
  
  const audio = new Audio(sounds[level]);
  audio.play();
};
```

---

## 五、交互设计

### 5.1 告警处理流程

```
新告警产生
    ↓
显示在待处理列表（实时推送）
    ↓
运营人员查看详情
    ↓
┌─────────────────────────────────┐
│ 确认告警                        │
│ - 表示已知晓，开始处理          │
│ - 记录确认时间和确认人          │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ 处理告警                        │
│ - 执行建议操作或自定义处理      │
│ - 可选：创建工单、指派他人      │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│ 解决告警                        │
│ - 记录解决方案                  │
│ - 标记处理结果                  │
└─────────────────────────────────┘
    ↓
告警关闭，进入历史记录
```

### 5.2 批量处理流程

```
选择多个告警（勾选）
    ↓
点击[批量处理]
    ↓
选择操作类型（确认/关闭/指派）
    ↓
填写批量操作参数
    ↓
确认执行
    ↓
显示处理结果（成功/失败数量）
```

### 5.3 告警升级机制

```
P1级别告警产生
    ↓
超过15分钟未确认
    ↓
自动升级为P0
    ↓
发送升级通知给上级主管
```

---

## 六、测试要求

### 6.1 单元测试

```typescript
describe('AlertList', () => {
  it('应正确渲染告警列表', () => {
    const alerts = mockAlerts(10);
    render(<AlertList alerts={alerts} {...defaultProps} />);
    expect(screen.getAllByRole('row')).toHaveLength(11);
  });

  it('应正确显示告警级别标签', () => {
    const alert = mockAlert({ level: 'P0' });
    render(<AlertList alerts={[alert]} {...defaultProps} />);
    expect(screen.getByText('P0')).toHaveClass('level-critical');
  });

  it('待处理告警应高亮显示', () => {
    const alert = mockAlert({ status: 'pending', level: 'P0' });
    render(<AlertList alerts={[alert]} {...defaultProps} />);
    expect(screen.getByTestId('alert-row-0')).toHaveClass('pending-highlight');
  });
});

describe('AlertDetail', () => {
  it('应显示告警详细信息', async () => {
    render(<AlertDetail alertId="alert-1" {...defaultProps} />);
    
    await waitFor(() => {
      expect(screen.getByText('机器人GX-003碰撞停机')).toBeInTheDocument();
    });
  });

  it('应显示建议操作', async () => {
    render(<AlertDetail alertId="alert-1" {...defaultProps} />);
    
    await waitFor(() => {
      expect(screen.getByText('现场检查机器人和碰撞位置')).toBeInTheDocument();
    });
  });
});

describe('AlertHandleModal', () => {
  it('解决告警时需要填写解决方案', async () => {
    const onSubmit = jest.fn();
    render(
      <AlertHandleModal 
        visible={true} 
        alert={mockAlert()} 
        action="resolve"
        onSubmit={onSubmit}
        onCancel={() => {}}
      />
    );
    
    fireEvent.click(screen.getByText('确认处理'));
    
    expect(await screen.findByText('请填写解决方案描述')).toBeInTheDocument();
    expect(onSubmit).not.toHaveBeenCalled();
  });
});
```

### 6.2 集成测试

```typescript
describe('AlertManagement Integration', () => {
  it('实时接收新告警', async () => {
    render(<AlertManagement />);
    
    // 模拟新告警
    mockWebSocket.emit('alert.new', {
      type: 'alert.new',
      alert: mockAlert({ id: 'new-alert', title: '新告警' })
    });
    
    await waitFor(() => {
      expect(screen.getByText('新告警')).toBeInTheDocument();
    });
  });

  it('完整的告警处理流程', async () => {
    render(<AlertManagement />);
    
    // 点击告警
    fireEvent.click(screen.getByText('机器人GX-003碰撞停机'));
    
    // 点击开始处理
    fireEvent.click(await screen.findByText('开始处理'));
    
    // 填写处理信息并提交
    fireEvent.change(screen.getByLabelText('处理备注'), {
      target: { value: '已派人现场处理' }
    });
    fireEvent.click(screen.getByText('确认'));
    
    // 验证状态更新
    await waitFor(() => {
      expect(screen.getByText('处理中')).toBeInTheDocument();
    });
  });
});
```

---

## 七、验收标准

### 7.1 功能验收

| 验收项 | 标准 |
|-------|------|
| 告警列表 | 正确显示告警列表，支持筛选和排序 |
| 实时推送 | 新告警实时显示，有声音和视觉提示 |
| 告警处理 | 可完成确认、处理、解决告警流程 |
| 批量操作 | 支持批量确认、关闭告警 |
| 告警规则 | 可配置告警规则和通知方式 |
| 告警统计 | 正确显示告警趋势和分布 |

### 7.2 性能要求

| 指标 | 要求 |
|-----|------|
| 列表加载 | < 500ms |
| 实时推送延迟 | < 2s |
| 处理操作响应 | < 1s |
| 统计图表加载 | < 1s |

### 7.3 可靠性要求

| 要求 | 说明 |
|-----|------|
| 不丢告警 | 即使网络断开重连也不丢失告警 |
| 提示机制 | P0/P1告警有明显的声音和视觉提示 |
| 操作确认 | 危险操作（如批量关闭）需确认 |

---

## 八、文件结构

```
src/pages/operations/alert-management/
├── index.tsx                    # 主页面
├── AlertManagement.module.css   # 样式
├── components/
│   ├── AlertOverview/
│   │   └── index.tsx
│   ├── AlertList/
│   │   ├── index.tsx
│   │   ├── AlertRow.tsx
│   │   └── LevelTag.tsx
│   ├── AlertFilter/
│   │   └── index.tsx
│   ├── AlertDetail/
│   │   ├── index.tsx
│   │   ├── SuggestedActions.tsx
│   │   └── HandleLogs.tsx
│   ├── AlertHandleModal/
│   │   └── index.tsx
│   ├── AlertRuleConfig/
│   │   ├── index.tsx
│   │   └── RuleEditModal.tsx
│   └── AlertStatistics/
│       ├── index.tsx
│       ├── TrendChart.tsx
│       └── DistributionChart.tsx
├── hooks/
│   ├── useAlertList.ts
│   ├── useAlertDetail.ts
│   ├── useAlertUpdates.ts
│   └── useAlertRules.ts
├── stores/
│   ├── alertStore.ts
│   └── alertRuleStore.ts
└── __tests__/
    ├── AlertList.test.tsx
    ├── AlertDetail.test.tsx
    └── integration.test.tsx
```

---

**规格书结束**
