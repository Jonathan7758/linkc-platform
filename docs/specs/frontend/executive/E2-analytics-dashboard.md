# E2 数据分析界面规格书

## 文档信息

| 属性 | 值 |
|-----|-----|
| 模块ID | E2 |
| 模块名称 | 数据分析界面 (Analytics Dashboard) |
| 版本 | 1.0 |
| 日期 | 2026-01-20 |
| 状态 | 规划中 |
| 所属终端 | 战略驾驶舱 (Executive Dashboard) |
| 前置依赖 | G6-data-api |

---

## 一、模块概述

### 1.1 职责描述

数据分析界面为管理层提供深度的运营数据分析能力，支持多维度数据探索、趋势分析、对比分析和预测分析，帮助管理层发现洞察、验证假设和支持决策。

### 1.2 核心功能

| 功能 | 描述 | 优先级 |
|-----|------|-------|
| 效率分析 | 清洁效率多维度分析 | P0 |
| 成本分析 | 运营成本详细分析 | P0 |
| 趋势分析 | 历史趋势和预测 | P0 |
| 对比分析 | 楼宇/机器人/时段对比 | P1 |
| 异常分析 | 异常事件分析 | P1 |
| 自定义报表 | 自定义指标和维度 | P2 |

### 1.3 分析维度

| 维度 | 说明 | 可选值 |
|-----|------|-------|
| 时间 | 分析时间范围 | 日/周/月/季/年/自定义 |
| 空间 | 分析空间范围 | 全部/楼宇/楼层/区域 |
| 设备 | 分析设备范围 | 全部/品牌/型号/单台 |
| 指标 | 分析指标类型 | 效率/成本/质量/异常 |

---

## 二、页面结构

### 2.1 主页面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [数据分析]                                                        [导出]   │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ [效率分析] [成本分析] [趋势分析] [对比分析] [异常分析]               │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │ 筛选条件                                                              │  │
│  │ [时间: 近30天▼] [楼宇: 全部▼] [机器人: 全部▼]  [应用筛选] [重置]     │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 效率分析仪表板                                                      │   │
│  │ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐            │   │
│  │ │ 平均覆盖率│ │ 平均完成率│ │ 平均效率  │ │ 机器人利用│            │   │
│  │ │  94.2%   │ │  96.8%   │ │  1.2x    │ │  78.5%   │            │   │
│  │ │ 目标:95% │ │ ↑2.1%   │ │ ↑0.1x   │ │ ↑5.2%   │            │   │
│  │ └───────────┘ └───────────┘ └───────────┘ └───────────┘            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────┐ ┌─────────────────────────────────┐   │
│  │ 效率趋势图                      │ │ 效率分布                        │   │
│  │ ┌─────────────────────────────┐│ │ ┌─────────────────────────────┐ │   │
│  │ │        📈                   ││ │ │                             │ │   │
│  │ │   /\    /\    /\           ││ │ │   ████                       │ │   │
│  │ │  /  \  /  \  /  \          ││ │ │   ████ ████                  │ │   │
│  │ │ /    \/    \/    \         ││ │ │   ████ ████ ████             │ │   │
│  │ │                             ││ │ │   A栋  B栋  C栋  D栋        │ │   │
│  │ └─────────────────────────────┘│ │ └─────────────────────────────┘ │   │
│  │ [日▼] 覆盖率 完成率 效率       │ │ 按楼宇分布                       │   │
│  └─────────────────────────────────┘ └─────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────┐ ┌─────────────────────────────────┐   │
│  │ 时段分析                        │ │ 效率明细                        │   │
│  │ ┌─────────────────────────────┐│ │ ┌─────────────────────────────┐ │   │
│  │ │ 热力图显示各时段效率        ││ │ │ 楼宇 | 覆盖率| 完成率| 趋势 │ │   │
│  │ │                             ││ │ │ A栋 | 96.5% | 98.2% | ↑    │ │   │
│  │ │ 06 08 10 12 14 16 18 20    ││ │ │ B栋 | 94.1% | 97.5% | →    │ │   │
│  │ │ ■■ ■■ ■■ ■■ ■■ ■■ ■■ ■■    ││ │ │ C栋 | 88.2% | 92.1% | ↓    │ │   │
│  │ │ 周一-周日                   ││ │ │ D栋 | 95.8% | 98.8% | ↑    │ │   │
│  │ └─────────────────────────────┘│ │ └─────────────────────────────┘ │   │
│  └─────────────────────────────────┘ └─────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、分析模块定义

### 3.1 效率分析 (EfficiencyAnalysis)

```typescript
interface EfficiencyAnalysisProps {
  filters: AnalysisFilters;
  onFilterChange: (filters: AnalysisFilters) => void;
}

interface AnalysisFilters {
  dateRange: [string, string];
  buildingIds?: string[];
  floorIds?: string[];
  robotIds?: string[];
  taskTypes?: string[];
}

interface EfficiencyData {
  // 汇总指标
  summary: {
    avgCoverageRate: number;
    avgCompletionRate: number;
    avgEfficiencyIndex: number;
    robotUtilization: number;
    trends: {
      coverage: number;
      completion: number;
      efficiency: number;
      utilization: number;
    };
  };
  // 趋势数据
  trend: {
    date: string;
    coverageRate: number;
    completionRate: number;
    efficiencyIndex: number;
  }[];
  // 按楼宇分布
  byBuilding: {
    buildingId: string;
    buildingName: string;
    coverageRate: number;
    completionRate: number;
    taskCount: number;
    trend: 'up' | 'down' | 'stable';
  }[];
  // 按时段分布
  byHour: {
    hour: number;
    dayOfWeek: number;
    avgEfficiency: number;
  }[];
  // 按机器人分布
  byRobot: {
    robotId: string;
    robotName: string;
    taskCount: number;
    avgEfficiency: number;
    utilization: number;
  }[];
}
```

### 3.2 成本分析 (CostAnalysis)

```typescript
interface CostAnalysisData {
  // 成本汇总
  summary: {
    totalCost: number;
    laborCost: number;
    robotCost: number;
    maintenanceCost: number;
    energyCost: number;
    costPerSquareMeter: number;
  };
  // 成本节约
  savings: {
    totalSaving: number;
    laborSaving: number;
    efficiencySaving: number;
    energySaving: number;
    roi: number;
  };
  // 成本趋势
  trend: {
    date: string;
    totalCost: number;
    laborCost: number;
    robotCost: number;
  }[];
  // 成本分布
  breakdown: {
    category: string;
    amount: number;
    percentage: number;
    trend: number;
  }[];
  // 按楼宇成本
  byBuilding: {
    buildingId: string;
    buildingName: string;
    totalCost: number;
    costPerSqm: number;
    efficiency: number;
  }[];
}
```

**成本分析布局**：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 成本分析                                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐     │
│  │ 总成本    │ │ 人力成本  │ │ 机器人成本│ │ 维护成本  │ │ 单位成本  │     │
│  │ ¥52.3万  │ │ ¥28.1万  │ │ ¥15.6万  │ │ ¥5.2万   │ │ ¥2.1/㎡  │     │
│  │ 本月      │ │ 53.7%    │ │ 29.8%    │ │ 9.9%     │ │ ↓8.3%   │     │
│  └───────────┘ └───────────┘ └───────────┘ └───────────┘ └───────────┘     │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────┐ ┌─────────────────────────────────┐   │
│  │ 成本节约分析                    │ │ 成本结构                        │   │
│  │ ┌─────────────────────────────┐│ │ ┌─────────────────────────────┐ │   │
│  │ │ 本月总节约: ¥12.5万         ││ │ │        🥧                    │ │   │
│  │ │ ├─ 人力替代: ¥8.2万 (65.6%)││ │ │    人力 54%                  │ │   │
│  │ │ ├─ 效率提升: ¥3.1万 (24.8%)││ │ │    机器人 30%                │ │   │
│  │ │ └─ 能耗优化: ¥1.2万 (9.6%) ││ │ │    维护 10%                  │ │   │
│  │ │                             ││ │ │    能耗 6%                   │ │   │
│  │ │ ROI: 285%                   ││ │ └─────────────────────────────┘ │   │
│  │ └─────────────────────────────┘│ │                                 │   │
│  └─────────────────────────────────┘ └─────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 成本趋势 (近12个月)                                                 │   │
│  │ [趋势折线图：总成本、人力成本、机器人成本]                           │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 趋势分析 (TrendAnalysis)

```typescript
interface TrendAnalysisData {
  // 历史趋势
  historicalTrend: {
    date: string;
    metric: number;
    movingAverage: number;
  }[];
  // 同比环比
  comparison: {
    current: number;
    previousPeriod: number;
    previousYear: number;
    momChange: number;  // 环比
    yoyChange: number;  // 同比
  };
  // 预测
  forecast: {
    date: string;
    predicted: number;
    upperBound: number;
    lowerBound: number;
  }[];
  // 季节性分析
  seasonality: {
    dayOfWeek: {
      day: number;
      avgValue: number;
    }[];
    hourOfDay: {
      hour: number;
      avgValue: number;
    }[];
  };
}
```

### 3.4 对比分析 (ComparisonAnalysis)

```typescript
interface ComparisonAnalysisProps {
  compareType: 'building' | 'robot' | 'period';
  items: string[];
  metric: string;
  dateRange: [string, string];
}

interface ComparisonData {
  items: {
    id: string;
    name: string;
    metrics: {
      [key: string]: number;
    };
    trend: number;
    rank: number;
  }[];
  // 对比图表数据
  chartData: {
    category: string;
    values: { [itemId: string]: number };
  }[];
}
```

**对比分析布局**：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 对比分析                                                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│  对比类型: ○楼宇对比 ●机器人对比 ○时段对比                                  │
│  选择对象: [✓]GX-001 [✓]GX-002 [✓]EC-001 [ ]GX-003 ...                     │
│  对比指标: [效率指数▼]                                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 对比图表                                                            │   │
│  │ ┌─────────────────────────────────────────────────────────────────┐ │   │
│  │ │         GX-001 ━━━━                                             │ │   │
│  │ │         GX-002 ----                                             │ │   │
│  │ │         EC-001 ····                                             │ │   │
│  │ │   /\      /\                                                    │ │   │
│  │ │  /  \    /  \    /\                                            │ │   │
│  │ │ /    \  /    \  /  \                                           │ │   │
│  │ │       \/      \/    \                                          │ │   │
│  │ │ 1/1   1/7   1/14   1/21                                        │ │   │
│  │ └─────────────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ 对比明细                                                            │   │
│  │ 机器人  | 任务数 | 覆盖率 | 完成率 | 效率 | 利用率 | 排名           │   │
│  │ GX-001  |  125  | 96.5% | 98.2% | 1.3x | 82.1% | #1              │   │
│  │ EC-001  |  118  | 95.2% | 97.8% | 1.2x | 79.5% | #2              │   │
│  │ GX-002  |  110  | 93.8% | 96.5% | 1.1x | 75.2% | #3              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.5 异常分析 (AnomalyAnalysis)

```typescript
interface AnomalyAnalysisData {
  // 异常统计
  summary: {
    totalAnomalies: number;
    byLevel: { [level: string]: number };
    byType: { [type: string]: number };
    avgResolutionTime: number;
    trend: number;
  };
  // 异常趋势
  trend: {
    date: string;
    count: number;
    p0: number;
    p1: number;
    p2: number;
    p3: number;
  }[];
  // 异常分布
  distribution: {
    byBuilding: { building: string; count: number }[];
    byRobot: { robot: string; count: number }[];
    byType: { type: string; count: number }[];
    byHour: { hour: number; count: number }[];
  };
  // Top异常
  topAnomalies: {
    type: string;
    count: number;
    avgResolutionTime: number;
    trend: number;
  }[];
  // 根因分析
  rootCauses: {
    cause: string;
    count: number;
    percentage: number;
    suggestion: string;
  }[];
}
```

---

## 四、图表组件

### 4.1 趋势折线图 (TrendLineChart)

```typescript
interface TrendLineChartProps {
  data: { date: string; [key: string]: number | string }[];
  lines: {
    key: string;
    name: string;
    color: string;
  }[];
  xAxisKey: string;
  height?: number;
  showLegend?: boolean;
  showGrid?: boolean;
}

const TrendLineChart: React.FC<TrendLineChartProps> = ({
  data, lines, xAxisKey, height = 300, showLegend = true, showGrid = true
}) => {
  return (
    <ResponsiveContainer width="100%" height={height}>
      <LineChart data={data}>
        {showGrid && <CartesianGrid strokeDasharray="3 3" />}
        <XAxis dataKey={xAxisKey} />
        <YAxis />
        <Tooltip />
        {showLegend && <Legend />}
        {lines.map(line => (
          <Line
            key={line.key}
            type="monotone"
            dataKey={line.key}
            name={line.name}
            stroke={line.color}
            dot={false}
          />
        ))}
      </LineChart>
    </ResponsiveContainer>
  );
};
```

### 4.2 分布柱状图 (DistributionBarChart)

```typescript
interface DistributionBarChartProps {
  data: { name: string; value: number; [key: string]: any }[];
  xAxisKey: string;
  barKey: string;
  height?: number;
  color?: string;
  onClick?: (item: any) => void;
}
```

### 4.3 热力图 (HeatmapChart)

```typescript
interface HeatmapChartProps {
  data: {
    x: number | string;
    y: number | string;
    value: number;
  }[];
  xLabels: string[];
  yLabels: string[];
  colorRange: [string, string];
  height?: number;
}
```

### 4.4 饼图/环图 (PieChart)

```typescript
interface PieChartProps {
  data: { name: string; value: number; color?: string }[];
  innerRadius?: number;
  outerRadius?: number;
  showLabel?: boolean;
  showLegend?: boolean;
}
```

---

## 五、数据流设计

### 5.1 状态管理

```typescript
// Analytics Store
interface AnalyticsStore {
  // 当前分析模块
  activeModule: 'efficiency' | 'cost' | 'trend' | 'comparison' | 'anomaly';
  
  // 筛选条件
  filters: AnalysisFilters;
  
  // 各模块数据
  efficiencyData: EfficiencyData | null;
  costData: CostAnalysisData | null;
  trendData: TrendAnalysisData | null;
  comparisonData: ComparisonData | null;
  anomalyData: AnomalyAnalysisData | null;
  
  // 加载状态
  loading: boolean;
  error: string | null;
  
  // Actions
  setActiveModule: (module: string) => void;
  setFilters: (filters: AnalysisFilters) => void;
  fetchEfficiencyData: () => Promise<void>;
  fetchCostData: () => Promise<void>;
  fetchTrendData: (metric: string) => Promise<void>;
  fetchComparisonData: (params: ComparisonParams) => Promise<void>;
  fetchAnomalyData: () => Promise<void>;
  exportData: (format: 'csv' | 'excel') => Promise<void>;
}
```

### 5.2 API调用

```typescript
const analyticsApi = {
  // 效率分析
  getEfficiencyAnalysis: (filters: AnalysisFilters): Promise<EfficiencyData> =>
    api.get('/api/v1/analytics/efficiency', { params: filters }),
  
  // 成本分析
  getCostAnalysis: (filters: AnalysisFilters): Promise<CostAnalysisData> =>
    api.get('/api/v1/analytics/cost', { params: filters }),
  
  // 趋势分析
  getTrendAnalysis: (metric: string, filters: AnalysisFilters): Promise<TrendAnalysisData> =>
    api.get('/api/v1/analytics/trend', { params: { metric, ...filters } }),
  
  // 对比分析
  getComparisonAnalysis: (params: ComparisonParams): Promise<ComparisonData> =>
    api.get('/api/v1/analytics/comparison', { params }),
  
  // 异常分析
  getAnomalyAnalysis: (filters: AnalysisFilters): Promise<AnomalyAnalysisData> =>
    api.get('/api/v1/analytics/anomaly', { params: filters }),
  
  // 导出
  exportAnalysis: (module: string, format: string, filters: AnalysisFilters): Promise<Blob> =>
    api.get(`/api/v1/analytics/${module}/export`, { 
      params: { format, ...filters },
      responseType: 'blob'
    }),
};
```

---

## 六、测试要求

### 6.1 单元测试

```typescript
describe('EfficiencyAnalysis', () => {
  it('应正确渲染效率指标卡片', () => {
    render(<EfficiencyAnalysis filters={defaultFilters} />);
    
    expect(screen.getByText('平均覆盖率')).toBeInTheDocument();
    expect(screen.getByText('平均完成率')).toBeInTheDocument();
  });

  it('筛选条件变化应触发数据刷新', async () => {
    const { rerender } = render(<EfficiencyAnalysis filters={defaultFilters} />);
    
    const newFilters = { ...defaultFilters, buildingIds: ['building-1'] };
    rerender(<EfficiencyAnalysis filters={newFilters} />);
    
    await waitFor(() => {
      expect(analyticsApi.getEfficiencyAnalysis).toHaveBeenCalledWith(newFilters);
    });
  });
});

describe('TrendLineChart', () => {
  it('应正确渲染多条折线', () => {
    const data = mockTrendData();
    const lines = [
      { key: 'coverage', name: '覆盖率', color: '#1890ff' },
      { key: 'completion', name: '完成率', color: '#52c41a' }
    ];
    
    render(<TrendLineChart data={data} lines={lines} xAxisKey="date" />);
    
    expect(screen.getByText('覆盖率')).toBeInTheDocument();
    expect(screen.getByText('完成率')).toBeInTheDocument();
  });
});
```

### 6.2 集成测试

```typescript
describe('Analytics Integration', () => {
  it('切换分析模块应加载对应数据', async () => {
    render(<AnalyticsDashboard />);
    
    // 切换到成本分析
    fireEvent.click(screen.getByText('成本分析'));
    
    await waitFor(() => {
      expect(analyticsApi.getCostAnalysis).toHaveBeenCalled();
    });
  });

  it('导出功能应下载文件', async () => {
    render(<AnalyticsDashboard />);
    
    fireEvent.click(screen.getByText('导出'));
    fireEvent.click(screen.getByText('导出Excel'));
    
    await waitFor(() => {
      expect(analyticsApi.exportAnalysis).toHaveBeenCalled();
    });
  });
});
```

---

## 七、验收标准

### 7.1 功能验收

| 验收项 | 标准 |
|-------|------|
| 效率分析 | 正确显示效率指标、趋势、分布 |
| 成本分析 | 正确显示成本结构、节约、ROI |
| 趋势分析 | 正确显示历史趋势和预测 |
| 对比分析 | 支持多对象多指标对比 |
| 数据筛选 | 筛选条件正确过滤数据 |
| 数据导出 | 支持CSV和Excel导出 |

### 7.2 性能要求

| 指标 | 要求 |
|-----|------|
| 页面加载 | < 2s |
| 筛选响应 | < 1s |
| 图表渲染 | < 500ms |
| 导出生成 | < 5s (常规数据量) |

---

## 八、文件结构

```
src/pages/executive/analytics/
├── index.tsx                    # 主页面
├── Analytics.module.css         # 样式
├── components/
│   ├── AnalyticsFilter/
│   │   └── index.tsx
│   ├── EfficiencyAnalysis/
│   │   ├── index.tsx
│   │   ├── EfficiencyMetrics.tsx
│   │   └── EfficiencyCharts.tsx
│   ├── CostAnalysis/
│   │   ├── index.tsx
│   │   ├── CostMetrics.tsx
│   │   └── SavingsBreakdown.tsx
│   ├── TrendAnalysis/
│   │   ├── index.tsx
│   │   └── ForecastChart.tsx
│   ├── ComparisonAnalysis/
│   │   ├── index.tsx
│   │   └── ComparisonTable.tsx
│   ├── AnomalyAnalysis/
│   │   ├── index.tsx
│   │   └── RootCauseAnalysis.tsx
│   └── Charts/
│       ├── TrendLineChart.tsx
│       ├── DistributionBarChart.tsx
│       ├── HeatmapChart.tsx
│       └── PieChart.tsx
├── hooks/
│   ├── useAnalyticsData.ts
│   └── useExport.ts
├── stores/
│   └── analyticsStore.ts
└── __tests__/
    └── analytics.test.tsx
```

---

**规格书结束**
