# P2 移动端任务规格书

## 文档信息
- **模块ID**: P2
- **模块名称**: 移动端任务 (Mobile Tasks)
- **版本**: 1.0
- **日期**: 2026年1月
- **状态**: 规划中
- **前置依赖**: G3任务API, P1移动端监控, O2任务管理

---

## 1. 模块概述

### 1.1 职责描述

移动端任务是LinkC平台移动端的任务管理模块，为运营人员提供移动办公的任务管理能力：
- 查看任务列表和执行状态
- 快速创建临时任务
- 监控任务执行进度
- 处理任务异常
- 审批任务相关请求

### 1.2 核心功能

| 功能 | 描述 | 优先级 |
|-----|------|-------|
| 任务列表 | 查看所有任务及状态 | P0 |
| 任务详情 | 查看任务详细信息和执行情况 | P0 |
| 快速创建 | 创建即时清洁任务 | P0 |
| 任务干预 | 暂停/恢复/取消任务 | P0 |
| 执行监控 | 实时查看任务执行进度 | P0 |
| 任务审批 | 审批策略变更等请求 | P1 |
| 历史查询 | 查看历史任务记录 | P2 |

### 1.3 目标用户

| 角色 | 使用场景 | 关注重点 |
|-----|---------|---------|
| 现场运维 | 创建临时任务、处理异常 | 快速创建、执行状态 |
| 运营经理 | 监控任务执行、审批请求 | 完成率、效率、审批 |
| 值班人员 | 夜间任务监控 | 异常任务、告警 |

### 1.4 设计原则

```
移动端任务设计原则：
├── 快速创建：3步内完成任务创建
├── 状态清晰：一眼识别任务状态
├── 操作便捷：常用操作单击可达
├── 信息聚焦：只显示必要信息
└── 实时同步：任务状态实时更新
```

---

## 2. 页面结构

### 2.1 任务模块结构

```
移动端任务模块结构：
├── 任务首页（Tasks Home）
│   ├── 今日概览
│   ├── 快捷入口
│   └── 进行中任务
├── 任务列表（Task List）
│   ├── 分类Tab
│   ├── 任务卡片列表
│   └── 筛选排序
├── 任务详情（Task Detail）
│   ├── 基本信息
│   ├── 执行进度
│   ├── 分配的机器人
│   └── 操作按钮
├── 创建任务（Create Task）
│   ├── 区域选择
│   ├── 任务类型
│   └── 执行配置
└── 审批中心（Approvals）
    ├── 待审批列表
    └── 审批详情
```

### 2.2 Tab导航设计

```typescript
// 任务模块不作为独立Tab，而是通过首页入口访问
// 可以从P1的更多菜单或仪表盘快捷入口进入

// 任务列表内部Tab配置
interface TaskTabConfig {
  tabs: TaskTab[];
}

interface TaskTab {
  key: TaskTabKey;
  title: string;
  badge?: number;
  filter: TaskFilter;
}

type TaskTabKey = 'today' | 'active' | 'scheduled' | 'completed' | 'failed';

const taskTabs: TaskTab[] = [
  { key: 'today', title: '今日', filter: { dateRange: 'today' } },
  { key: 'active', title: '进行中', badge: 5, filter: { status: ['in_progress', 'paused'] } },
  { key: 'scheduled', title: '待执行', filter: { status: ['pending'] } },
  { key: 'completed', title: '已完成', filter: { status: ['completed'] } },
  { key: 'failed', title: '异常', badge: 2, filter: { status: ['failed', 'cancelled'] } }
];
```

---

## 3. 数据模型

### 3.1 任务数据结构

```typescript
// 任务摘要（列表用）
interface TaskSummary {
  id: string;
  name: string;
  type: TaskType;
  status: TaskStatus;
  priority: TaskPriority;
  
  // 位置信息
  location: {
    building: string;
    buildingName: string;
    floor?: string;
    floorName?: string;
    zones?: string[];
  };
  
  // 执行信息
  scheduledTime: string;
  startTime?: string;
  endTime?: string;
  progress: number;          // 0-100
  
  // 分配的机器人
  robots: {
    total: number;
    active: number;
    robotNames: string[];
  };
  
  // 异常信息
  hasAlert: boolean;
  alertCount?: number;
}

// 任务详情
interface TaskDetail extends TaskSummary {
  description?: string;
  
  // 详细配置
  config: {
    cleaningMode: CleaningMode;
    intensity: CleaningIntensity;
    waterUsage: WaterUsage;
    repeatConfig?: RepeatConfig;
  };
  
  // 机器人详情
  assignedRobots: AssignedRobot[];
  
  // 执行详情
  execution: {
    totalArea: number;           // 计划面积 m²
    cleanedArea: number;         // 已清洁面积 m²
    coverageRate: number;        // 覆盖率
    estimatedCompletion?: string; // 预计完成时间
    actualDuration?: number;     // 实际耗时 分钟
  };
  
  // 时间线
  timeline: TaskTimelineEvent[];
  
  // 创建信息
  createdBy: string;
  createdAt: string;
  updatedAt: string;
}

interface AssignedRobot {
  id: string;
  name: string;
  status: RobotTaskStatus;
  progress: number;
  zone?: string;
  zoneName?: string;
}

type RobotTaskStatus = 
  | 'pending'      // 等待
  | 'working'      // 工作中
  | 'completed'    // 已完成
  | 'failed'       // 失败
  | 'paused';      // 已暂停

interface TaskTimelineEvent {
  id: string;
  type: TimelineEventType;
  title: string;
  description?: string;
  timestamp: string;
  actor?: string;    // 操作人/机器人
}

type TimelineEventType = 
  | 'created'
  | 'started'
  | 'robot_assigned'
  | 'robot_started'
  | 'robot_completed'
  | 'paused'
  | 'resumed'
  | 'alert'
  | 'completed'
  | 'failed'
  | 'cancelled';
```

### 3.2 任务类型定义

```typescript
type TaskType = 
  | 'routine'        // 日常清洁
  | 'deep'           // 深度清洁
  | 'spot'           // 定点清洁
  | 'emergency';     // 紧急清洁

type TaskStatus = 
  | 'pending'        // 待执行
  | 'in_progress'    // 执行中
  | 'paused'         // 已暂停
  | 'completed'      // 已完成
  | 'failed'         // 失败
  | 'cancelled';     // 已取消

type TaskPriority = 'low' | 'normal' | 'high' | 'urgent';

type CleaningMode = 'vacuum' | 'mop' | 'vacuum_mop' | 'dust';
type CleaningIntensity = 'light' | 'standard' | 'intensive';
type WaterUsage = 'low' | 'medium' | 'high';

// 任务状态配置
const taskStatusConfig: Record<TaskStatus, { label: string; color: string; icon: string }> = {
  pending: { label: '待执行', color: '#8c8c8c', icon: 'clock-outline' },
  in_progress: { label: '执行中', color: '#1890ff', icon: 'play-circle' },
  paused: { label: '已暂停', color: '#faad14', icon: 'pause-circle' },
  completed: { label: '已完成', color: '#52c41a', icon: 'checkmark-circle' },
  failed: { label: '失败', color: '#ff4d4f', icon: 'close-circle' },
  cancelled: { label: '已取消', color: '#8c8c8c', icon: 'ban' }
};

// 任务类型配置
const taskTypeConfig: Record<TaskType, { label: string; icon: string }> = {
  routine: { label: '日常清洁', icon: 'calendar' },
  deep: { label: '深度清洁', icon: 'water' },
  spot: { label: '定点清洁', icon: 'locate' },
  emergency: { label: '紧急清洁', icon: 'flash' }
};
```

---

## 4. 任务首页

### 4.1 今日概览

```typescript
// 今日任务概览数据
interface TodayTaskOverview {
  // 统计数据
  stats: {
    total: number;           // 总任务数
    completed: number;       // 已完成
    inProgress: number;      // 进行中
    pending: number;         // 待执行
    failed: number;          // 失败
  };
  
  // 完成率
  completionRate: number;    // 0-100
  
  // 效率指标
  efficiency: {
    avgDuration: number;     // 平均耗时 分钟
    onTimeRate: number;      // 准时完成率
  };
  
  // 当前进行中任务
  activeTasks: TaskSummary[];
}

// 今日概览组件Props
interface TodayOverviewProps {
  data: TodayTaskOverview;
  onViewAll: () => void;
  onTaskPress: (taskId: string) => void;
}

// 今日概览布局
/*
┌───────────────────────────────────────┐
│  今日任务                      查看全部>│
│  ─────────────────────────────────    │
│  ┌─────────────────────────────────┐  │
│  │  完成 12/15    进行中 2   待执行 1 │  │
│  │  ████████████░░░  80%           │  │
│  └─────────────────────────────────┘  │
│                                       │
│  进行中任务                           │
│  ┌─────────────────────────────────┐  │
│  │ 🔵 A栋日常清洁-上午             │  │
│  │    进度 67%  │  3台机器人       │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ 🔵 B栋大堂深度清洁              │  │
│  │    进度 23%  │  1台机器人       │  │
│  └─────────────────────────────────┘  │
└───────────────────────────────────────┘
*/
```

### 4.2 快捷入口

```typescript
// 快捷入口配置
interface QuickAction {
  key: string;
  title: string;
  icon: string;
  color: string;
  action: () => void;
}

const quickActions: QuickAction[] = [
  {
    key: 'create_spot',
    title: '定点清洁',
    icon: 'locate',
    color: '#1890ff',
    action: () => navigation.navigate('CreateTask', { type: 'spot' })
  },
  {
    key: 'create_emergency',
    title: '紧急清洁',
    icon: 'flash',
    color: '#ff4d4f',
    action: () => navigation.navigate('CreateTask', { type: 'emergency' })
  },
  {
    key: 'view_schedule',
    title: '今日排程',
    icon: 'calendar',
    color: '#52c41a',
    action: () => navigation.navigate('TaskList', { tab: 'today' })
  },
  {
    key: 'view_alerts',
    title: '异常任务',
    icon: 'alert-circle',
    color: '#faad14',
    action: () => navigation.navigate('TaskList', { tab: 'failed' })
  }
];

// 快捷入口布局
/*
┌───────────────────────────────────────┐
│  快捷操作                             │
│  ┌─────────┐ ┌─────────┐             │
│  │   📍    │ │   ⚡    │             │
│  │定点清洁 │ │紧急清洁 │             │
│  └─────────┘ └─────────┘             │
│  ┌─────────┐ ┌─────────┐             │
│  │   📅    │ │   ⚠️    │             │
│  │今日排程 │ │异常任务 │             │
│  └─────────┘ └─────────┘             │
└───────────────────────────────────────┘
*/
```

---

## 5. 任务列表页

### 5.1 任务列表组件

```typescript
// 任务列表Props
interface TaskListScreenProps {
  initialTab?: TaskTabKey;
}

// 任务列表状态
interface TaskListState {
  currentTab: TaskTabKey;
  tasks: TaskSummary[];
  loading: boolean;
  refreshing: boolean;
  filter: TaskFilter;
  sort: TaskSort;
  pagination: {
    page: number;
    hasMore: boolean;
  };
}

interface TaskFilter {
  dateRange?: 'today' | 'week' | 'month' | 'custom';
  startDate?: string;
  endDate?: string;
  status?: TaskStatus[];
  type?: TaskType[];
  building?: string;
  priority?: TaskPriority[];
  search?: string;
}

interface TaskSort {
  field: 'scheduledTime' | 'priority' | 'status' | 'progress';
  order: 'asc' | 'desc';
}
```

### 5.2 任务卡片组件

```typescript
// 任务卡片Props
interface TaskCardProps {
  task: TaskSummary;
  onPress: () => void;
  onQuickAction?: (action: QuickTaskAction) => void;
}

type QuickTaskAction = 'pause' | 'resume' | 'cancel';

// 任务卡片布局
/*
正常任务:
┌───────────────────────────────────────┐
│ 🔵 A栋日常清洁-上午场次      10:00   │
│    ─────────────────────────────      │
│    A栋 · 3F-5F · 日常清洁             │
│                                       │
│    ████████████░░░░░░ 67%            │
│                                       │
│    🤖 3台 │ 预计 11:30 完成          │
│                                   >  │
└───────────────────────────────────────┘

异常任务:
┌───────────────────────────────────────┐
│ 🔴 B栋深度清洁               09:00   │
│    ─────────────────────────────      │
│    ⚠️ 1台机器人故障，任务暂停         │
│    B栋 · 1F 大堂 · 深度清洁           │
│                                       │
│    ████████░░░░░░░░░░ 35%            │
│                                       │
│    🤖 2台 │ 查看详情 >>              │
└───────────────────────────────────────┘

待执行任务:
┌───────────────────────────────────────┐
│ ⏳ C栋日常清洁-下午场次      14:00   │
│    ─────────────────────────────      │
│    C栋 · 全楼层 · 日常清洁            │
│                                       │
│    🤖 4台已分配 │ 2小时后开始         │
│                                   >  │
└───────────────────────────────────────┘
*/
```

### 5.3 筛选器组件

```typescript
// 筛选器Props
interface TaskFilterSheetProps {
  visible: boolean;
  filter: TaskFilter;
  buildings: Building[];
  onApply: (filter: TaskFilter) => void;
  onClose: () => void;
  onReset: () => void;
}

// 筛选器布局
/*
┌───────────────────────────────────────┐
│  筛选任务                      重置   │
│  ─────────────────────────────────    │
│  时间范围                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│  │ 今日│ │本周 │ │本月 │ │自定义│    │
│  └─────┘ └─────┘ └─────┘ └─────┘    │
│                                       │
│  任务状态                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│  │执行中│ │待执行│ │已完成│ │ 失败│    │
│  └─────┘ └─────┘ └─────┘ └─────┘    │
│                                       │
│  任务类型                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│  │ 日常│ │ 深度│ │ 定点│ │ 紧急│    │
│  └─────┘ └─────┘ └─────┘ └─────┘    │
│                                       │
│  楼宇                                 │
│  ┌─────────────────────────────┐     │
│  │ 全部楼宇                   ▼│     │
│  └─────────────────────────────┘     │
│                                       │
│  ┌─────────────────────────────────┐ │
│  │           应用筛选              │ │
│  └─────────────────────────────────┘ │
└───────────────────────────────────────┘
*/
```

---

## 6. 任务详情页

### 6.1 详情页结构

```typescript
// 任务详情页Props
interface TaskDetailScreenProps {
  taskId: string;
}

// 详情页Section配置
const detailSections = [
  { key: 'status', title: '执行状态' },
  { key: 'progress', title: '执行进度' },
  { key: 'robots', title: '执行机器人' },
  { key: 'config', title: '任务配置' },
  { key: 'timeline', title: '执行时间线' }
];
```

### 6.2 执行状态卡片

```typescript
// 执行状态组件
interface TaskStatusCardProps {
  task: TaskDetail;
  onAction: (action: TaskAction) => void;
}

type TaskAction = 
  | { type: 'pause' }
  | { type: 'resume' }
  | { type: 'cancel' }
  | { type: 'restart' };

// 执行状态布局
/*
┌───────────────────────────────────────┐
│  执行状态                             │
│  ─────────────────────────────────    │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │  🔵 执行中                      │  │
│  │  ─────────────────────────      │  │
│  │  开始: 10:00  │  预计完成: 11:30│  │
│  │  已耗时: 45分钟                 │  │
│  └─────────────────────────────────┘  │
│                                       │
│  ┌─────────┐ ┌─────────┐            │
│  │  ⏸️    │ │  ⏹️    │            │
│  │  暂停   │ │  取消   │            │
│  └─────────┘ └─────────┘            │
│                                       │
└───────────────────────────────────────┘

暂停状态:
┌───────────────────────────────────────┐
│  执行状态                             │
│  ─────────────────────────────────    │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │  🟡 已暂停                      │  │
│  │  ─────────────────────────      │  │
│  │  暂停原因: 机器人R-005故障      │  │
│  │  暂停时间: 10:45               │  │
│  └─────────────────────────────────┘  │
│                                       │
│  ┌─────────┐ ┌─────────┐            │
│  │  ▶️    │ │  ⏹️    │            │
│  │  恢复   │ │  取消   │            │
│  └─────────┘ └─────────┘            │
│                                       │
└───────────────────────────────────────┘
*/
```

### 6.3 执行进度组件

```typescript
// 执行进度组件
interface TaskProgressCardProps {
  execution: TaskDetail['execution'];
  robots: AssignedRobot[];
}

// 执行进度布局
/*
┌───────────────────────────────────────┐
│  执行进度                             │
│  ─────────────────────────────────    │
│                                       │
│  总体进度                             │
│  ████████████████░░░░░░░░ 67%        │
│                                       │
│  ┌───────────┐ ┌───────────┐         │
│  │ 500 m²    │ │ 335 m²    │         │
│  │ 计划面积   │ │ 已清洁    │         │
│  └───────────┘ └───────────┘         │
│  ┌───────────┐ ┌───────────┐         │
│  │ 95%       │ │ 45分钟    │         │
│  │ 覆盖率    │ │ 已耗时    │         │
│  └───────────┘ └───────────┘         │
│                                       │
└───────────────────────────────────────┘
*/
```

### 6.4 机器人列表组件

```typescript
// 机器人列表组件
interface TaskRobotsCardProps {
  robots: AssignedRobot[];
  onRobotPress: (robotId: string) => void;
}

// 机器人列表布局
/*
┌───────────────────────────────────────┐
│  执行机器人                    共3台   │
│  ─────────────────────────────────    │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │ 🤖 R-005    │  🟢 工作中        │  │
│  │    区域: 3F东区 │ 进度: 80%     │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ 🤖 R-008    │  🟢 工作中        │  │
│  │    区域: 3F西区 │ 进度: 65%     │  │
│  └─────────────────────────────────┘  │
│  ┌─────────────────────────────────┐  │
│  │ 🤖 R-012    │  🔴 故障          │  │
│  │    区域: 4F │ 进度: 45%        │  │
│  └─────────────────────────────────┘  │
│                                       │
└───────────────────────────────────────┘
*/
```

### 6.5 时间线组件

```typescript
// 时间线组件
interface TaskTimelineCardProps {
  timeline: TaskTimelineEvent[];
  onEventPress?: (event: TaskTimelineEvent) => void;
}

// 时间线事件图标配置
const timelineEventConfig: Record<TimelineEventType, { icon: string; color: string }> = {
  created: { icon: 'add-circle', color: '#8c8c8c' },
  started: { icon: 'play-circle', color: '#1890ff' },
  robot_assigned: { icon: 'robot', color: '#1890ff' },
  robot_started: { icon: 'play', color: '#52c41a' },
  robot_completed: { icon: 'checkmark', color: '#52c41a' },
  paused: { icon: 'pause-circle', color: '#faad14' },
  resumed: { icon: 'play-circle', color: '#1890ff' },
  alert: { icon: 'alert-circle', color: '#ff4d4f' },
  completed: { icon: 'checkmark-circle', color: '#52c41a' },
  failed: { icon: 'close-circle', color: '#ff4d4f' },
  cancelled: { icon: 'ban', color: '#8c8c8c' }
};

// 时间线布局
/*
┌───────────────────────────────────────┐
│  执行时间线                           │
│  ─────────────────────────────────    │
│                                       │
│  ● 10:45  R-012故障，任务暂停         │
│  │        机器人在4F走廊卡住          │
│  │                                    │
│  ● 10:30  R-008开始执行              │
│  │        分配区域: 3F西区            │
│  │                                    │
│  ● 10:15  R-005开始执行              │
│  │        分配区域: 3F东区            │
│  │                                    │
│  ● 10:00  任务开始执行               │
│  │        3台机器人已分配             │
│  │                                    │
│  ● 09:55  任务创建                   │
│           创建人: 系统自动            │
│                                       │
└───────────────────────────────────────┘
*/
```

---

## 7. 创建任务页

### 7.1 创建任务流程

```typescript
// 创建任务页Props
interface CreateTaskScreenProps {
  type?: TaskType;  // 预设类型
  building?: string; // 预设楼宇
  zone?: string;    // 预设区域
}

// 创建任务表单
interface CreateTaskForm {
  // 步骤1：选择区域
  building: string;
  floors: string[];
  zones: string[];
  
  // 步骤2：任务配置
  type: TaskType;
  name: string;
  priority: TaskPriority;
  
  // 步骤3：执行配置
  cleaningMode: CleaningMode;
  intensity: CleaningIntensity;
  waterUsage: WaterUsage;
  scheduledTime: 'now' | 'scheduled';
  scheduleDateTime?: string;
}

// 创建任务步骤
type CreateTaskStep = 'location' | 'config' | 'schedule' | 'confirm';
```

### 7.2 区域选择步骤

```typescript
// 区域选择组件
interface LocationStepProps {
  form: CreateTaskForm;
  buildings: Building[];
  onUpdate: (updates: Partial<CreateTaskForm>) => void;
  onNext: () => void;
}

// 区域选择布局
/*
┌───────────────────────────────────────┐
│ ← 创建任务         步骤 1/4          │
├───────────────────────────────────────┤
│                                       │
│  选择清洁区域                         │
│  ─────────────────────────────────    │
│                                       │
│  楼宇 *                               │
│  ┌─────────────────────────────────┐  │
│  │ 选择楼宇                      ▼ │  │
│  └─────────────────────────────────┘  │
│                                       │
│  楼层                                 │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│  │ 全选│ │ 1F  │ │ 2F  │ │ 3F  │    │
│  └─────┘ └─────┘ └─────┘ └─────┘    │
│  ┌─────┐ ┌─────┐ ┌─────┐            │
│  │ 4F  │ │ 5F  │ │ 6F  │            │
│  └─────┘ └─────┘ └─────┘            │
│                                       │
│  区域（可选，不选则清洁整层）          │
│  ┌─────────┐ ┌─────────┐            │
│  │  大堂   │ │  走廊   │            │
│  └─────────┘ └─────────┘            │
│  ┌─────────┐ ┌─────────┐            │
│  │ 电梯厅  │ │ 洗手间  │            │
│  └─────────┘ └─────────┘            │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │           下一步                │  │
│  └─────────────────────────────────┘  │
└───────────────────────────────────────┘
*/
```

### 7.3 任务配置步骤

```typescript
// 任务配置组件
interface ConfigStepProps {
  form: CreateTaskForm;
  onUpdate: (updates: Partial<CreateTaskForm>) => void;
  onBack: () => void;
  onNext: () => void;
}

// 任务配置布局
/*
┌───────────────────────────────────────┐
│ ← 创建任务         步骤 2/4          │
├───────────────────────────────────────┤
│                                       │
│  任务配置                             │
│  ─────────────────────────────────    │
│                                       │
│  任务类型 *                           │
│  ┌─────────┐ ┌─────────┐            │
│  │ ✓ 日常 │ │   深度  │            │
│  └─────────┘ └─────────┘            │
│  ┌─────────┐ ┌─────────┐            │
│  │   定点  │ │   紧急  │            │
│  └─────────┘ └─────────┘            │
│                                       │
│  任务名称                             │
│  ┌─────────────────────────────────┐  │
│  │ A栋3-5F日常清洁                 │  │
│  └─────────────────────────────────┘  │
│                                       │
│  优先级                               │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│  │ 低  │ │✓普通│ │ 高  │ │紧急 │    │
│  └─────┘ └─────┘ └─────┘ └─────┘    │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │           下一步                │  │
│  └─────────────────────────────────┘  │
└───────────────────────────────────────┘
*/
```

### 7.4 执行配置步骤

```typescript
// 执行配置组件
interface ScheduleStepProps {
  form: CreateTaskForm;
  onUpdate: (updates: Partial<CreateTaskForm>) => void;
  onBack: () => void;
  onNext: () => void;
}

// 执行配置布局
/*
┌───────────────────────────────────────┐
│ ← 创建任务         步骤 3/4          │
├───────────────────────────────────────┤
│                                       │
│  执行配置                             │
│  ─────────────────────────────────    │
│                                       │
│  清洁模式 *                           │
│  ┌─────────┐ ┌─────────┐            │
│  │ ✓ 吸尘 │ │  拖地   │            │
│  └─────────┘ └─────────┘            │
│  ┌─────────┐ ┌─────────┐            │
│  │吸尘+拖地│ │  除尘   │            │
│  └─────────┘ └─────────┘            │
│                                       │
│  清洁强度                             │
│  [ ○────────●────────○ ]             │
│    轻度      标准      强力           │
│                                       │
│  执行时间                             │
│  ○ 立即执行                           │
│  ● 定时执行                           │
│  ┌─────────────────────────────────┐  │
│  │ 2026-01-20 14:00            📅 │  │
│  └─────────────────────────────────┘  │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │           下一步                │  │
│  └─────────────────────────────────┘  │
└───────────────────────────────────────┘
*/
```

### 7.5 确认步骤

```typescript
// 确认组件
interface ConfirmStepProps {
  form: CreateTaskForm;
  buildings: Building[];
  onBack: () => void;
  onConfirm: () => void;
  submitting: boolean;
}

// 确认布局
/*
┌───────────────────────────────────────┐
│ ← 创建任务         步骤 4/4          │
├───────────────────────────────────────┤
│                                       │
│  确认任务信息                         │
│  ─────────────────────────────────    │
│                                       │
│  任务名称                             │
│  A栋3-5F日常清洁                      │
│                                       │
│  清洁区域                             │
│  A栋 · 3F, 4F, 5F · 全区域           │
│                                       │
│  任务类型                             │
│  日常清洁 · 普通优先级                │
│                                       │
│  清洁配置                             │
│  吸尘模式 · 标准强度                  │
│                                       │
│  执行时间                             │
│  定时执行 · 2026-01-20 14:00         │
│                                       │
│  预计信息                             │
│  预计分配 3 台机器人                  │
│  预计耗时 90 分钟                     │
│  预计面积 800 m²                      │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │         确认创建任务            │  │
│  └─────────────────────────────────┘  │
└───────────────────────────────────────┘
*/
```

---

## 8. 审批中心

### 8.1 待审批列表

```typescript
// 审批项数据结构
interface ApprovalItem {
  id: string;
  type: ApprovalType;
  title: string;
  description: string;
  requester: string;
  requestTime: string;
  urgency: 'normal' | 'urgent';
  status: 'pending' | 'approved' | 'rejected';
  data: ApprovalData;
}

type ApprovalType = 
  | 'schedule_change'    // 排程变更
  | 'policy_change'      // 策略变更
  | 'robot_assignment'   // 机器人调配
  | 'task_cancel';       // 任务取消

interface ApprovalData {
  // 根据type不同有不同的数据结构
  [key: string]: any;
}

// 审批列表组件
interface ApprovalListProps {
  approvals: ApprovalItem[];
  onItemPress: (approval: ApprovalItem) => void;
  onRefresh: () => void;
  refreshing: boolean;
}

// 审批列表布局
/*
┌───────────────────────────────────────┐
│  待审批                        共5项   │
│  ─────────────────────────────────    │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │ 🔴 紧急 排程变更               │  │
│  │ A栋日常清洁时间调整              │  │
│  │ 申请人: 张经理 │ 10分钟前       │  │
│  └─────────────────────────────────┘  │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │ 策略变更                        │  │
│  │ B栋清洁强度调整                  │  │
│  │ 申请人: 李主管 │ 30分钟前       │  │
│  └─────────────────────────────────┘  │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │ 机器人调配                      │  │
│  │ R-005临时调往C栋                │  │
│  │ 申请人: Agent │ 1小时前        │  │
│  └─────────────────────────────────┘  │
│                                       │
└───────────────────────────────────────┘
*/
```

### 8.2 审批详情

```typescript
// 审批详情组件
interface ApprovalDetailProps {
  approval: ApprovalItem;
  onApprove: (comment?: string) => void;
  onReject: (reason: string) => void;
  submitting: boolean;
}

// 审批详情布局
/*
┌───────────────────────────────────────┐
│ ← 审批详情                            │
├───────────────────────────────────────┤
│                                       │
│  排程变更申请                         │
│  ─────────────────────────────────    │
│                                       │
│  申请信息                             │
│  申请人: 张经理                       │
│  申请时间: 2026-01-20 10:30          │
│  紧急程度: 🔴 紧急                    │
│                                       │
│  变更内容                             │
│  ┌─────────────────────────────────┐  │
│  │ 任务: A栋日常清洁-上午          │  │
│  │ ─────────────────────────       │  │
│  │ 原时间: 10:00 - 12:00           │  │
│  │ 新时间: 09:00 - 11:00           │  │
│  │ ─────────────────────────       │  │
│  │ 变更原因:                        │  │
│  │ 上午10点有重要客户参观，需要     │  │
│  │ 提前完成大堂清洁工作             │  │
│  └─────────────────────────────────┘  │
│                                       │
│  影响评估                             │
│  • 需要提前调度3台机器人              │
│  • 可能影响B栋清洁任务开始时间        │
│                                       │
│  ┌─────────────────────────────────┐  │
│  │ 审批意见（可选）                 │  │
│  │                                 │  │
│  └─────────────────────────────────┘  │
│                                       │
│  ┌───────────┐  ┌───────────┐       │
│  │    拒绝   │  │    通过   │       │
│  └───────────┘  └───────────┘       │
└───────────────────────────────────────┘
*/
```

---

## 9. 状态管理

### 9.1 任务Store

```typescript
// 使用 Zustand 管理状态
import create from 'zustand';

interface MobileTaskStore {
  // 今日概览
  todayOverview: TodayTaskOverview | null;
  todayLoading: boolean;
  fetchTodayOverview: () => Promise<void>;
  
  // 任务列表
  tasks: TaskSummary[];
  tasksLoading: boolean;
  taskFilter: TaskFilter;
  taskSort: TaskSort;
  setTaskFilter: (filter: TaskFilter) => void;
  setTaskSort: (sort: TaskSort) => void;
  fetchTasks: () => Promise<void>;
  loadMoreTasks: () => Promise<void>;
  
  // 任务详情
  currentTask: TaskDetail | null;
  currentTaskLoading: boolean;
  fetchTaskDetail: (taskId: string) => Promise<void>;
  
  // 任务操作
  createTask: (form: CreateTaskForm) => Promise<string>;
  pauseTask: (taskId: string) => Promise<void>;
  resumeTask: (taskId: string) => Promise<void>;
  cancelTask: (taskId: string, reason?: string) => Promise<void>;
  
  // 审批
  approvals: ApprovalItem[];
  approvalsLoading: boolean;
  pendingApprovalCount: number;
  fetchApprovals: () => Promise<void>;
  approveItem: (id: string, comment?: string) => Promise<void>;
  rejectItem: (id: string, reason: string) => Promise<void>;
  
  // WebSocket
  subscribeToTaskUpdates: (taskId: string) => void;
  unsubscribeFromTaskUpdates: (taskId: string) => void;
}

const useMobileTaskStore = create<MobileTaskStore>((set, get) => ({
  // 初始状态
  todayOverview: null,
  todayLoading: false,
  tasks: [],
  tasksLoading: false,
  taskFilter: {},
  taskSort: { field: 'scheduledTime', order: 'asc' },
  currentTask: null,
  currentTaskLoading: false,
  approvals: [],
  approvalsLoading: false,
  pendingApprovalCount: 0,
  
  // Actions
  fetchTodayOverview: async () => {
    set({ todayLoading: true });
    try {
      const data = await taskApi.getTodayOverview();
      set({ todayOverview: data, todayLoading: false });
    } catch (error) {
      set({ todayLoading: false });
      throw error;
    }
  },
  
  fetchTasks: async () => {
    set({ tasksLoading: true });
    const { taskFilter, taskSort } = get();
    try {
      const data = await taskApi.getTasks({ ...taskFilter, sort: taskSort });
      set({ tasks: data.items, tasksLoading: false });
    } catch (error) {
      set({ tasksLoading: false });
      throw error;
    }
  },
  
  createTask: async (form) => {
    const result = await taskApi.createTask(form);
    // 刷新任务列表和今日概览
    get().fetchTasks();
    get().fetchTodayOverview();
    return result.taskId;
  },
  
  pauseTask: async (taskId) => {
    await taskApi.pauseTask(taskId);
    // 更新当前任务状态
    if (get().currentTask?.id === taskId) {
      get().fetchTaskDetail(taskId);
    }
    get().fetchTasks();
  },
  
  // ... 其他action实现
}));
```

### 9.2 API服务

```typescript
// 任务API服务
interface TaskApi {
  // 今日概览
  getTodayOverview(): Promise<TodayTaskOverview>;
  
  // 任务列表
  getTasks(params: TaskQueryParams): Promise<PaginatedResponse<TaskSummary>>;
  getTaskDetail(taskId: string): Promise<TaskDetail>;
  
  // 任务操作
  createTask(form: CreateTaskForm): Promise<{ taskId: string }>;
  pauseTask(taskId: string): Promise<void>;
  resumeTask(taskId: string): Promise<void>;
  cancelTask(taskId: string, reason?: string): Promise<void>;
  
  // 审批
  getApprovals(): Promise<ApprovalItem[]>;
  approveItem(id: string, comment?: string): Promise<void>;
  rejectItem(id: string, reason: string): Promise<void>;
}

interface TaskQueryParams extends TaskFilter {
  sort?: TaskSort;
  page?: number;
  pageSize?: number;
}

// API实现
const taskApi: TaskApi = {
  getTodayOverview: () =>
    fetch('/api/v1/mobile/tasks/today-overview').then(r => r.json()),
  
  getTasks: (params) =>
    fetch(`/api/v1/mobile/tasks?${new URLSearchParams(params as any)}`).then(r => r.json()),
  
  getTaskDetail: (taskId) =>
    fetch(`/api/v1/mobile/tasks/${taskId}`).then(r => r.json()),
  
  createTask: (form) =>
    fetch('/api/v1/mobile/tasks', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(form)
    }).then(r => r.json()),
  
  pauseTask: (taskId) =>
    fetch(`/api/v1/mobile/tasks/${taskId}/pause`, { method: 'POST' }).then(r => r.json()),
  
  resumeTask: (taskId) =>
    fetch(`/api/v1/mobile/tasks/${taskId}/resume`, { method: 'POST' }).then(r => r.json()),
  
  cancelTask: (taskId, reason) =>
    fetch(`/api/v1/mobile/tasks/${taskId}/cancel`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ reason })
    }).then(r => r.json()),
  
  // ... 其他实现
};
```

---

## 10. 测试要求

### 10.1 单元测试

```typescript
describe('MobileTaskStore', () => {
  beforeEach(() => {
    useMobileTaskStore.setState({
      tasks: [],
      todayOverview: null,
      currentTask: null,
      approvals: []
    });
  });
  
  it('should fetch today overview', async () => {
    const mockOverview: TodayTaskOverview = {
      stats: { total: 15, completed: 12, inProgress: 2, pending: 1, failed: 0 },
      completionRate: 80,
      efficiency: { avgDuration: 60, onTimeRate: 0.95 },
      activeTasks: []
    };
    
    jest.spyOn(taskApi, 'getTodayOverview').mockResolvedValue(mockOverview);
    
    await useMobileTaskStore.getState().fetchTodayOverview();
    
    expect(useMobileTaskStore.getState().todayOverview).toEqual(mockOverview);
  });
  
  it('should create task and refresh data', async () => {
    const mockForm: CreateTaskForm = {
      building: 'bldg-001',
      floors: ['floor-1', 'floor-2'],
      zones: [],
      type: 'routine',
      name: 'Test Task',
      priority: 'normal',
      cleaningMode: 'vacuum',
      intensity: 'standard',
      waterUsage: 'medium',
      scheduledTime: 'now'
    };
    
    jest.spyOn(taskApi, 'createTask').mockResolvedValue({ taskId: 'task-123' });
    jest.spyOn(taskApi, 'getTasks').mockResolvedValue({ items: [], total: 0 });
    jest.spyOn(taskApi, 'getTodayOverview').mockResolvedValue({} as any);
    
    const taskId = await useMobileTaskStore.getState().createTask(mockForm);
    
    expect(taskId).toBe('task-123');
    expect(taskApi.getTasks).toHaveBeenCalled();
    expect(taskApi.getTodayOverview).toHaveBeenCalled();
  });
});

describe('TaskCard', () => {
  it('should render task status correctly', () => {
    const task: TaskSummary = {
      id: 'task-1',
      name: 'Test Task',
      type: 'routine',
      status: 'in_progress',
      priority: 'normal',
      location: { building: 'bldg-1', buildingName: 'A栋' },
      scheduledTime: new Date().toISOString(),
      progress: 67,
      robots: { total: 3, active: 3, robotNames: ['R-001', 'R-002', 'R-003'] },
      hasAlert: false
    };
    
    const { getByText } = render(<TaskCard task={task} onPress={jest.fn()} />);
    
    expect(getByText('Test Task')).toBeTruthy();
    expect(getByText('执行中')).toBeTruthy();
    expect(getByText('67%')).toBeTruthy();
  });
  
  it('should show alert indicator for failed task', () => {
    const task: TaskSummary = {
      ...baseTask,
      status: 'failed',
      hasAlert: true,
      alertCount: 2
    };
    
    const { getByTestId } = render(<TaskCard task={task} onPress={jest.fn()} />);
    
    expect(getByTestId('alert-indicator')).toBeTruthy();
  });
});
```

### 10.2 集成测试

```typescript
describe('Mobile Task E2E', () => {
  beforeAll(async () => {
    await device.launchApp();
  });
  
  it('should navigate to task list from quick action', async () => {
    await element(by.id('quick-action-schedule')).tap();
    await expect(element(by.id('task-list-screen'))).toBeVisible();
  });
  
  it('should create a spot cleaning task', async () => {
    // 进入创建任务
    await element(by.id('quick-action-spot')).tap();
    await expect(element(by.id('create-task-screen'))).toBeVisible();
    
    // 步骤1：选择区域
    await element(by.id('building-select')).tap();
    await element(by.text('A栋')).tap();
    await element(by.id('floor-3')).tap();
    await element(by.id('next-button')).tap();
    
    // 步骤2：任务配置
    await element(by.id('task-name-input')).typeText('临时定点清洁');
    await element(by.id('next-button')).tap();
    
    // 步骤3：执行配置
    await element(by.id('execute-now')).tap();
    await element(by.id('next-button')).tap();
    
    // 步骤4：确认
    await element(by.id('confirm-create')).tap();
    
    // 验证创建成功
    await expect(element(by.text('任务创建成功'))).toBeVisible();
  });
  
  it('should pause and resume a task', async () => {
    // 进入任务详情
    await element(by.id('task-item-0')).tap();
    await expect(element(by.id('task-detail-screen'))).toBeVisible();
    
    // 暂停任务
    await element(by.id('pause-button')).tap();
    await expect(element(by.text('已暂停'))).toBeVisible();
    
    // 恢复任务
    await element(by.id('resume-button')).tap();
    await expect(element(by.text('执行中'))).toBeVisible();
  });
});
```

---

## 11. 文件结构

```
src/mobile/tasks/
├── screens/
│   ├── TaskHomeScreen.tsx           # 任务首页
│   ├── TaskListScreen.tsx           # 任务列表
│   ├── TaskDetailScreen.tsx         # 任务详情
│   ├── CreateTaskScreen.tsx         # 创建任务
│   └── ApprovalScreen.tsx           # 审批中心
├── components/
│   ├── home/
│   │   ├── TodayOverview.tsx
│   │   └── QuickActions.tsx
│   ├── list/
│   │   ├── TaskCard.tsx
│   │   ├── TaskTabs.tsx
│   │   └── TaskFilter.tsx
│   ├── detail/
│   │   ├── TaskStatusCard.tsx
│   │   ├── TaskProgressCard.tsx
│   │   ├── TaskRobotsCard.tsx
│   │   ├── TaskTimelineCard.tsx
│   │   └── TaskActionSheet.tsx
│   ├── create/
│   │   ├── LocationStep.tsx
│   │   ├── ConfigStep.tsx
│   │   ├── ScheduleStep.tsx
│   │   └── ConfirmStep.tsx
│   └── approval/
│       ├── ApprovalList.tsx
│       └── ApprovalDetail.tsx
├── store/
│   └── mobileTaskStore.ts
├── services/
│   └── taskApi.ts
├── hooks/
│   ├── useTaskList.ts
│   ├── useTaskDetail.ts
│   └── useApprovals.ts
├── utils/
│   ├── taskFormatters.ts
│   └── taskValidation.ts
└── types/
    └── index.ts

tests/
├── unit/
│   ├── store.test.ts
│   └── components/
├── integration/
│   └── screens.test.ts
└── e2e/
    └── tasks.e2e.ts
```

---

## 12. 验收标准

### 12.1 功能验收

| 功能 | 验收标准 |
|-----|---------|
| 今日概览 | 显示任务统计、完成率、进行中任务 |
| 任务列表 | 分类展示、支持筛选和搜索 |
| 任务详情 | 显示完整信息、执行进度、时间线 |
| 创建任务 | 3步完成任务创建，支持立即和定时执行 |
| 任务操作 | 成功执行暂停/恢复/取消操作 |
| 审批功能 | 查看待审批列表，完成审批操作 |

### 12.2 性能验收

| 指标 | 验收标准 |
|-----|---------|
| 列表加载 | < 1秒 |
| 创建任务 | < 2秒 |
| 状态更新 | 实时（WebSocket） |

---

*文档版本: 1.0*  
*创建日期: 2026年1月*  
*最后更新: 2026年1月*
